meta {
  name: List Orders Newest First
  method: GET
  url: {{baseUrl}}/api/orders?page=0&size=20&sort=newest
  type: http
}

get {
  url: {{baseUrl}}/api/orders?page=0&size=20&sort=newest
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
