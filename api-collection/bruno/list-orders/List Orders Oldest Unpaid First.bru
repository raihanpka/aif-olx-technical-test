meta {
  name: List Orders Oldest Unpaid First
  method: GET
  url: {{baseUrl}}/api/orders?page=0&size=20&sort=oldest_unpaid
  type: http
}

get {
  url: {{baseUrl}}/api/orders?page=0&size=20&sort=oldest_unpaid
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
